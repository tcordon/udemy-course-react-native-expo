import { StyleSheet } from 'react-native'

export const style = StyleSheet.create({
  content: {
    position: 'absolute',
    top: 0,
    right: 0,
    backgroundColor: '#fff',
    padding: 5,
    paddingLeft: 10,
    borderBottomLeftRadius: 20
  }
})
